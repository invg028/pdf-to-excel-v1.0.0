# PDF → Excel Pro (Next.js + TypeScript, OCR)

- PDF digital via PDF.js
- Fallback OCR via Tesseract.js (ind+eng)
- Ekspor XLSX via SheetJS

## Lokal
1) `npm i`
2) `npm run dev`
3) http://localhost:3000

## Vercel
- Import repo → build default (`next build`) → Deploy.
